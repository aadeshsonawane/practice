#include <stdio.h>

int main()
{
	int l, h;
	printf("Enter the length: ");
	scanf("%d", &l);
	printf("Enter the hight: ");
	scanf("%d", &h);
	
	printf("area of rectangle is %d\n", l*h);
	return(0);


} 
